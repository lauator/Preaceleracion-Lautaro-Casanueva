<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.cinemapp.views.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_detailDialogFragment"
            app:destination="@id/detailDialogFragment" />
    </fragment>

    <dialog
        android:id="@+id/detailDialogFragment"
        android:name="com.example.cinemapp.views.detail.DetailDialogFragment"
        android:label="DetailDialogFragment"
        tools:layout="@layout/fragment_detail_dialog"
        >

    </dialog>
</navigation>